# 🦊 切换 MetaMask 到 Hardhat Local 网络

## ❌ 当前问题

错误信息：
```
RelayerSDKLoader: window object does not contain a valid relayerSDK object.
```

**原因**：MetaMask 连接到了 Sepolia 测试网，但应该连接到本地 Hardhat 网络。

---

## ✅ 解决步骤

### 方法 1：手动添加 Hardhat Local 网络

#### 步骤 1：打开 MetaMask 设置
1. 点击 MetaMask 右上角的 **三个点** (⋯)
2. 选择 **"设置"** (Settings)

#### 步骤 2：添加网络
1. 左侧菜单选择 **"网络"** (Networks)
2. 点击 **"添加网络"** (Add a network) 按钮
3. 点击 **"手动添加网络"** (Add a network manually)

#### 步骤 3：填写网络信息
```
网络名称 (Network name):
Hardhat Local

RPC URL:
http://localhost:8545

链 ID (Chain ID):
31337

货币符号 (Currency symbol):
ETH

区块浏览器 URL (可选):
(留空)
```

#### 步骤 4：保存并切换
1. 点击 **"保存"** (Save)
2. MetaMask 会自动切换到新添加的网络
3. 顶部应该显示 **"Hardhat Local"**

---

### 方法 2：使用 MetaMask 快速切换

如果之前已经添加过：

1. **点击 MetaMask 顶部的网络名称**
   - 当前可能显示 "Sepolia" 或其他网络

2. **在下拉列表中找到**：
   - "Hardhat Local" 或
   - "Localhost 31337"

3. **点击切换**
   - 顶部应该立即变为 "Hardhat Local"

---

## 🔍 验证切换成功

### 检查 1：MetaMask 顶部
```
✅ 应该显示：Hardhat Local
❌ 不应该显示：Sepolia、Ethereum Mainnet 等
```

### 检查 2：页面 Chain ID
刷新页面后，FHEVM Status 卡片应该显示：
```
FHEVM STATUS
Ready ✅
chainId 31337  ← 应该是 31337，不是 11155111
```

### 检查 3：账户余额
```
✅ 测试账户应该显示 ~10000 ETH
❌ 如果是 0 ETH，说明账户不对或网络不对
```

---

## 📋 完整检查清单

切换网络后，按 `Ctrl + F5` 刷新页面，确认：

- [ ] MetaMask 顶部显示 "Hardhat Local"
- [ ] Chain ID 显示 31337
- [ ] FHEVM Status 显示 "Ready ✅"
- [ ] 账户余额显示 ~10000 ETH
- [ ] 不再有 RelayerSDK 错误

---

## ⚠️ 常见问题

### Q1: 找不到 Hardhat Local 网络？
**A**: 按照 "方法 1" 手动添加，确保 RPC URL 是 `http://localhost:8545`，Chain ID 是 `31337`

### Q2: 切换后还是报错？
**A**: 
1. 刷新页面 (`Ctrl + F5`)
2. 检查 Hardhat 节点是否运行（应该有窗口显示日志）
3. 如果没有，运行 `E:\Spring\Zama\lucky\restart-services.bat`

### Q3: 切换后余额是 0？
**A**: 
1. 确认使用的是测试账户（`0xf39F...2266`）
2. 如果不是，导入测试账户（参考 `如何导入测试账户到MetaMask.md`）

### Q4: Hardhat 节点没有运行？
**A**: 
```batch
# 重启所有服务
cd E:\Spring\Zama\lucky
.\restart-services.bat
```

---

## 🎯 两个网络的区别

### Hardhat Local (本地测试)
```
✅ Chain ID: 31337
✅ RPC: http://localhost:8545
✅ 用途: 本地开发和测试
✅ 免费: 无需真实 ETH
✅ 快速: 即时出块
✅ FHEVM: 使用 Mock 模式（不需要 RelayerSDK）
```

### Sepolia (公共测试网)
```
⚠️ Chain ID: 11155111
⚠️ RPC: https://sepolia.infura.io/...
⚠️ 用途: 公共测试
⚠️ 需要: 测试网 ETH（需要从 faucet 获取）
⚠️ 慢速: 12-15 秒出块
⚠️ FHEVM: 需要 RelayerSDK（Zama 协处理器）
```

---

## 🚀 切换完成后

1. **刷新页面**：`Ctrl + F5`
2. **查看状态**：FHEVM Status 应该显示 "Ready ✅"
3. **开始游戏**：选择骰子值并提交投注

---

**现在切换网络，然后刷新页面试试！** 🎲

